/**
 * Shared Hooks
 * Uygulama genelinde kullanılan custom hooks
 * 
 * @example
 * import { useAlarm, useWeather } from '@/shared/hooks'
 */

// Alarm hook
export { useAlarm } from './useAlarm'

// Weather hook
export { useWeather } from './useWeather'

